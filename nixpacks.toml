[phases.setup]
nixPkgs = ["nodePackages.npm", "python311"]

[phases.install]
cmds = [
    "npm install",
    "python3 -m ensurepip",
    "python3 -m pip install -U pip",
    "python3 -m pip install -r requirements.txt"
]